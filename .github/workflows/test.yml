name: Test

on:
  workflow_call:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: gh-top-languages/.github/.github/actions/setup@v1.0.0

      - name: TypeScript typecheck
        run: npm run typecheck

      - name: Run tests
        run: npm run test:coverage
